
//time
$timer = 3000;
// element
$images = document.querySelectorAll("img");
// first image
$images[0].classList.add('show');
console.log($images);

$imgNum = 0;
function showImg{
    for($i=0; i < $images.length; i++){
        $images
    }
}